export type DeviceViewModel = {
    ip: string,
    title: string,
    lastActivateDate: string,
    deviceId: string,
}

export type DeviceDBModel = {
    ip: string,
    title: string,
    lastActivateDate: string,
    deviceId: string,
    userId: string,
}